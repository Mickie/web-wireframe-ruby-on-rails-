<%= form_for(@user_brag) do |f| %>
  <% if @user_brag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_brag.errors.count, "error") %> prohibited this user_brag from being saved:</h2>

      <ul>
      <% @user_brag.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user %><br />
    <%= f.text_field :user %>
  </div>
  <div class="field">
    <%= f.label :brag %><br />
    <%= f.text_field :brag %>
  </div>
  <div class="field">
    <%= f.label :type %><br />
    <%= f.number_field :type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
