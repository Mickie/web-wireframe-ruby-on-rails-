<%= form_for(@venue) do |f| %>
  <% if @venue.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@venue.errors.count, "error") %> prohibited this venue from being saved:</h2>

      <ul>
      <% @venue.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :venue_type %><br />
    <%= f.select(:venue_type_id, options_for_select(VenueType.all.collect {|p| [ p.name, p.id ] } )) %>
  </div>

  <%= render :partial => 'partials/social_info', :locals => { :f => f } %>
  <%= render :partial => 'partials/location', :locals => { :f => f } %>

  <div class="actions">
    <%= f.submit "Save", id:"commit" %>
  </div>
<% end %>
