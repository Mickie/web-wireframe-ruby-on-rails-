<%= form_for(@division) do |f| %>
  <% if @division.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@division.errors.count, "error") %> prohibited this division from being saved:</h2>

      <ul>
      <% @division.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :league %>
    <%= f.select(:league_id, options_for_select(League.all.collect {|p| [ p.name, p.id ] }, @division.league_id )) %>
  </div>
  
  <div class="actions">
    <%= f.submit "Save", id:"commit" %>
  </div>
<% end %>
