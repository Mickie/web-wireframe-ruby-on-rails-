$("div#frameContent").html("<%= escape_javascript( render partial: "partials/tiles/all_fanzone_tiles", locals:{ aUserSignedInFlag: user_signed_in? } ) -%>");
if (typeof myNavigator !== 'undefined')
{
  myNavigator.notifySearchComplete();
}
else
{
  myPhoneNavigator.notifySearchComplete();
}
