<%= form_for(current_user.tailgate_followers.build(tailgate_id: tailgate.id), remote: true) do |f| %>
  <%= f.hidden_field :tailgate_id %>
  <%= f.submit "Follow", class: "follow_button fanzoButton", id:"#{tailgate.id}", onclick:"return handleFollowClick(event);" %>
<% end %>