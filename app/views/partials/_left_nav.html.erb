<nav id="left_nav">
  
  <ul class="nav nav-tabs nav-stacked">
    <li id="logo">
      <a href="/">
        <div>
          <span class="brand">FANZO</span><span class="tld">.ME</span>
          <p class="tagline">
            where fans rule
          </p>
        </div>
      </a>
    </li>
    
    <li id="allFanZones">
      <a onclick="loadData('<%= tailgates_path(noLayout:true ) %>')" href="#">ALL FANZONES</a> 
    </li>
    
    <li>
      <a onclick="loadData('<%= tailgates_path(filter:"user", noLayout:true ) %>')" href="#">MY FANZONES</a> 
    </li>

    <% if signed_in? && current_user.tailgates.length != 0 %>
      <% current_user.tailgates.each do |aTailgate|%>
        <li>
          <a href="#" onclick="loadData('<%= tailgate_path(id:aTailgate, noLayout:true ) %>')">
            <%= teamLogo(aTailgate.team, :small) %>
            <%= aTailgate.name %>
          </a>
        </li>
      <% end %>
    <% end %>

    <li>
      <a href="<%= new_tailgate_path %>">
        <span class="create">+ &nbsp; CREATE A FANZONE</span>
      </a>
    </li>
    
  </ul>
</nav>
