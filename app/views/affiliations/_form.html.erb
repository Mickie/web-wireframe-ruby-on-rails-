<%= form_for(@affiliation) do |f| %>
  <% if @affiliation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@affiliation.errors.count, "error") %> prohibited this affiliation from being saved:</h2>

      <ul>
      <% @affiliation.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <%= render :partial => 'partials/social_info', :locals => { :f => f } %>
  <%= render :partial => 'partials/location', :locals => { :f => f, :location => @affiliation.location } %>
  
  <div class="actions">
    <%= f.submit "Save", id:"commit" %>
  </div>

<% end %>
