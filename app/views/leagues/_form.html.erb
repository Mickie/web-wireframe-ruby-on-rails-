<%= form_for(@league) do |f| %>
  <% if @league.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@league.errors.count, "error") %> prohibited this league from being saved:</h2>

      <ul>
      <% @league.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  
  <div>
    <%= f.select(:sport_id, options_for_select(@allSports.collect {|p| [ p.name, p.id ] },  "Select Sport")) %>
  </div>
  
  <div class="actions">
    <%= f.submit "Save", id:"commit" %>
  </div>
<% end %>
