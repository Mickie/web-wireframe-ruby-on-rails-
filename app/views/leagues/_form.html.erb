<%= form_for(@league) do |f| %>
  <% if @league.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@league.errors.count, "error") %> prohibited this league from being saved:</h2>

      <ul>
      <% @league.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  
  <div class="field">
    <%= f.label :sport %>
    <%= f.select(:sport_id, options_for_select(Sport.all.collect {|p| [ p.name, p.id ] }, @league.sport_id )) %>
  </div>
  
  <div class="field">
    <%= f.label :visible %>
    <%= f.check_box(:visible) %>
  </div>
  
  <div class="actions">
    <%= f.submit "Save", id:"commit" %>
  </div>
<% end %>
