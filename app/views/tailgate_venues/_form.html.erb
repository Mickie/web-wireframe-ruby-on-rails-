<%= form_for(@tailgate_venue) do |f| %>
  <% if @tailgate_venue.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tailgate_venue.errors.count, "error") %> prohibited this tailgate_venue from being saved:</h2>

      <ul>
      <% @tailgate_venue.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tailgate %><br />
    <%= f.select(:tailgate_id, options_for_select(Tailgate.order("created_at DESC").all.collect {|p| [ p.name, p.id ] }, @tailgate_venue.tailgate_id )) %>
  </div>
  <div class="field">
    <%= f.label :venue %><br />
    <%= f.select(:venue_id, options_for_select(Venue.order("created_at DESC").all.collect {|p| [ p.name, p.id ] }, @tailgate_venue.venue_id )) %>
  </div>
  <div class="actions">
    <%= f.submit "Save", id:"commit" %>
  </div>
<% end %>
