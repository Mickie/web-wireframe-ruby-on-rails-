<h1>Listing event_posts</h1>

<table>
  <tr>
    <th>Has visiting post</th>
    <th>Has home post</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @event_posts.each do |event_post| %>
  <tr>
    <td><%= event_post.visiting_flag %></td>
    <td><%= event_post.home_flag %></td>
    <td><%= link_to 'Show', [@event, event_post] %></td>
    <td><%= link_to 'Edit', edit_event_event_post_path(@event, event_post) %></td>
    <td><%= link_to 'Destroy', [@event, event_post], method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Event post', new_event_event_post_path(@event) %>
