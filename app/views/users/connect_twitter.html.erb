<%= provide(:title, "Connect Twitter To My Profile" ) %>
<p><%= @user.email if @user.email %></p>

<h1>To share your thoughts, you need to connect to Twitter</h1>

<%= render partial: "partials/buttons/connect_twitter_button" %>
