<%= form_for(@quick_tweet) do |f| %>
  <% if @quick_tweet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@quick_tweet.errors.count, "error") %> prohibited this quick_tweet from being saved:</h2>

      <ul>
      <% @quick_tweet.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :sport %><br />
    <%= f.select(:sport_id, options_for_select(Sport.all.collect {|p| [ p.name, p.id ] }, @quick_tweet.sport_id )) %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :tweet %><br />
    <%= f.text_field :tweet %>
  </div>
  <div class="field">
    <%= f.label :happy %><br />
    <%= f.check_box :happy %>
  </div>
  <div class="actions">
    <%= f.submit "Save", id:"commit" %>
  </div>

<% end %>
