<%= form_for(@watch_site) do |f| %>
  <% if @watch_site.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@watch_site.errors.count, "error") %> prohibited this watch_site from being saved:</h2>

      <ul>
      <% @watch_site.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :team %>
    <%= f.select(:team_id, options_for_select(Team.all.collect {|p| [ p.name, p.id ] }, @watch_site.team_id )) %>
  </div>
  <div class="field">
    <%= f.label :venue %>
    <%= f.select(:venue_id, options_for_select(Venue.order("created_at DESC").all.collect {|p| [ p.name, p.id ] }, @watch_site.venue_id )) %>
  </div>
  
  <div class="actions">
    <%= f.submit "Save", id:"commit" %>
  </div>

<% end %>
