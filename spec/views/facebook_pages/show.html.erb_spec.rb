require 'spec_helper'

describe "facebook_pages/show" do
  before(:each) do
    @facebook_page = assign(:facebook_page, stub_model(FacebookPage))
  end

  it "renders attributes in <p>" do
#    render
    # Run the generator again with the --webrat flag if you want to use webrat matchers
  end
end
